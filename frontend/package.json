{
  "name": "cloud-backup-client",
  "version": "0.1.0",
  "description": "Cloud Backup desktop client built with Electron",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "NODE_ENV=development electron .",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:debug": "playwright test --debug"
  },
  "keywords": [
    "cloud",
    "backup",
    "electron"
  ],
  "license": "GPL-3.0",
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "electron-reload": "^2.0.0-alpha.1",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "playwright": "^1.44.0"
  },
  "dependencies": {
    "electron": "^28.2.0"
  }
}
